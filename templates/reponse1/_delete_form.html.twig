<form method="post" action="{{ path('app_Reponse1_delete', {'id': Reponse1.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
	<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ Reponse1.id) }}">
	<button class="btn">Delete</button>
</form>
